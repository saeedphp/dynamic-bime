import {Fragment} from "react";

const SearchResult = () => {
    return (
        <Fragment>
            <ul>
                {/*{search.map((item, i) => {
                    return (
                        <li key={i}>
                            {item.title}
                        </li>
                    )
                })}*/}
            </ul>
        </Fragment>
    )
};

export default SearchResult;